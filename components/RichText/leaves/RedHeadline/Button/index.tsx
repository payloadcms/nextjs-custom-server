import React from 'react';
import { LeafButton } from 'payload/components/rich-text';

const Button = () => (
  <LeafButton format="red-headline">
    Red Headline
  </LeafButton>
);

export default Button;
