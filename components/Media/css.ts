import { createUseStyles } from 'react-jss';

export default createUseStyles({
  image: {
    // maxWidth: '100%',
    width: '100%',
    height:'100%'
  },
  video: {
    maxWidth: '100%',
    width: '100%',
    height:'100%',
  },
});
