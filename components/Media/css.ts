import { createUseStyles } from 'react-jss';

export default createUseStyles({
  image: {
    // maxWidth: '100%',
    width: '100%',
    height:'100%',
    borderRadius:'10px',
  },
  video: {
    maxWidth: '100%',
    width: '100%',
    height:'100%',
  },
});
